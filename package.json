{
  "name": "conectamos-emails",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preview": "email dev --dir emails",
    "build": "email export --outDir dist --pretty",
    "sync": "tsx scripts/sync-resend.ts",
    "sync:dry": "tsx scripts/sync-resend.ts --dry-run"
  },
  "dependencies": {
    "@react-email/components": "^0.0.22",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-email": "^3.0.1",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.1",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
